# datos crudos/curados (viven en S3, no en git) — solo la carpeta raíz,
# NO web/src/data/ (que tiene los datos mock del frontend)
/data/
*.csv
*.parquet
!core/agrupaciones/diccionario.csv
# secretos
.env
.env.*
*.pem
# python
__pycache__/
*.pyc
.venv/

# documentos de referencia del usuario (no versionar)
documentacion/
